import { CbuFormatPipe } from './cbu-format-pipe';

describe('CbuFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new CbuFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
